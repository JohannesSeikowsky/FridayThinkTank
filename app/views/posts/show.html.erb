<% provide(:title, "Thought of the week - Post") %> 

<div class="row">

  <div class="col-sm-1">
    <%= image_tag(@post.user.profile.image(:medium)) %>
  </div> 

  <div class="col-xs-12 col-sm-10 col-md-6 col-md-offset-3 post_show">
    <h3> <%= @post.title %> </h3>
    <p> <%= h(@post.content).gsub(/\n/, '<br/>').html_safe %> </p>
  </div>
</div> 

<br>

<div class="row">
  <div class="col-xs-12 col-sm-10 col-sm-offset-1 col-md-6 col-md-offset-3 post_show">
    <p> Add your comment </p>
    <%= form_for([@post,@new_comment], url: post_comments_path(@post)) do |f| %>
      <%= f.text_area :content, class: "form-control"%> <br> 
      <%= f.submit "Post", class: "btn btn-md btn-warning" %> 
    <% end %> 
      <% @post.comments.each do |comment| %> 
        <div style="margin-top: 30px;"> <%= comment.content %> </div>
      <% end %> 
    </div>
  </div>
</div> 


